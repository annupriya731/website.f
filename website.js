
let top = document.querySelectorAll('.topdiv');

let links = document.querySelectorAll('nav a');
window.onscroll = ( =>{


    sec.forEach(topdiv=>{
        let top= window.scrollY;
        let offset = topdiv.offsetTop;
        let height = topdiv.offsetheight;
        let id= topdiv.getattribute('id')

    })
})

